// app/dashboard/layout.tsx (if you keep dashboard folder)
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
    return (
        <div className='min-h-screen bg-gray-50'>
            Dashboard layout coming soon...
            {children}
        </div>
    );
}
